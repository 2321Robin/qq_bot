.worktrees/
.venv/
.agent
logs
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.env
.superpowers/

# Coverage reports (S1-COV-02: machine-readable report is an artifact, never committed)
.coverage
coverage.xml
htmlcov/

# Local runtime state (container smoke tests, ad-hoc databases)
container-data/

# docs/ — mostly ignored; only public-release markdown and screenshots tracked
docs/*
!docs/public-release/
!docs/assets/
docs/assets/*
!docs/assets/screenshots/

data/chat_memory.sqlite3
# Game data — not redistributable
/data/roco_pet_details/
/data/roco_assets/
/data/roco_pet_cards/
dist/
build/
*.egg-info/

# Local startup configuration (see startup.example.ps1)
startup.local.ps1

# Gitleaks scan reports (may contain secret patterns)
gitleaks-report/

# Project resume guide (contains personal project context)
PROJECT_RESUME_GUIDE.md

# Git bundle / backup
*.bundle
